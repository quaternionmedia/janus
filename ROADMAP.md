Roadmap of things to come.

2. tidy up the code, make more legible and easier to follow
3. provide some kind of image of pico 2 once implemnted
4. tray notification for larger copied content (if necessary after pico migration)
5. if #4, implement a "manual" grab from tray popup. Would work similar to controller's manual 'c'.
6. implement image clipboard (requires chunked protocol)
7. implement monitor layout configurations
8. go through a security pass
9. benchmark/metrics/stress test
10. write tests for what can be tested (ensure easy to run all together as much as possible, perhaps script to run both py and c# tests)


?. implement file-list clipboard (very unlikely, RDP does this by proxying the file through connection)


DONE: 
1. migrate to pico 2 (non-w) design